{
  "name": "js-tetris",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "ph-pages -d dist"
  },
  "devDependencies": {
    "vite": "^5.3.4"
  },
  "dependencies": {
    "standard": "^17.1.0"
  },
  "eslintConfig": {
    "extends":[
      "standard"
    ]
  }
}
